using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlashSystem : MonoBehaviour // TODO maybe merge this with explosion system, and make it support all the types of explosions?
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
